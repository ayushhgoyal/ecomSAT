<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#f9f8f8">

    <android.support.v7.widget.CardView
        android:id="@+id/cv_parent_category_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_margin="@dimen/card_view_margin_normal"
        android:elevation="@dimen/card_view_elevation_normal"
        android:visibility="gone"
        app:cardCornerRadius="@dimen/card_view_corner_radius_normal"
        app:contentPadding="@dimen/card_view_padding_normal">

        <RelativeLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">

            <ImageView
                android:id="@+id/back_icon"
                android:layout_width="wrap_content"
                android:layout_height="12dp"
                android:layout_alignBottom="@+id/tv_category_name"
                android:layout_alignParentLeft="true"
                android:layout_alignTop="@+id/tv_category_name"
                android:adjustViewBounds="true"
                android:padding="4dp"
                android:scaleType="fitStart"
                android:src="@drawable/back" />

            <TextView
                android:id="@+id/tv_category_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_toRightOf="@+id/back_icon"
                android:text="Parent"
                android:textSize="@dimen/text_normal"
                android:visibility="visible" />
        </RelativeLayout>


    </android.support.v7.widget.CardView>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rv_category_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/cv_parent_category_container"
        android:orientation="horizontal" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rv_product_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/rv_category_list"
        android:scrollbars="vertical" />
</RelativeLayout>